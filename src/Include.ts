export enum Include {
    /**
     * Include all defined properties.
     */
    DEFAULT = "DEFAULT",
    /**
     * Include all defined properties, except for null values.
     */
    NON_NULL = "NON_NULL"
}
